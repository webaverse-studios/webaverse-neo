/**
 * Regex for matching a https url
 *
 * @example
 * https://foo.bar
 */
export const httpsRegex = /^https?:\/\//

/**
 * Regex for absolute imports
 *
 * @example
 * /foo/bar
 */
export const absoluteImportRegex = /^\//
